import {Text} from "react-native";

const Card = (props: {item: number, key: any}) => {
    return (
        <>
            <Text>
                item
            </Text>
        </>
    )
}

export default Card;